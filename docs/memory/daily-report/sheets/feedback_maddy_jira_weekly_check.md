---
name: feedback_maddy_jira_weekly_check
description: "Maddy JIRA cross-check runs EVERY day with --week flag — 🔴 260715: script reads a STALE abandoned Sheet, not live Workstream, false-reports \"no tickets\" — verify live before trusting"
metadata:
  type: feedback
---

Run `node scripts/maddy-jira-tasklog-check.js --week [YYYY-MM-DD]` as part of **every** daily report Sheets piece. Never skip because LongVV has 0h today or Maddy logs are empty — the weekly check covers all logged tickets that week.

**Why:** User added this Jun 11 to catch tickets missing estimates, no JIRA log, or over-budget. Was originally gated on "LongVV > 0h" — wrong; check must always run.

**How to apply:**
- Script: `node scripts/maddy-jira-tasklog-check.js --week [YYYY-MM-DD]`
- Output: markdown table, append to daily report Sheets section under heading `## Sheets — Maddy JIRA — W{n} — {HH:MM} (+07:00)`
- Checks per ticket: (1) est set, (2) actual logged on JIRA, (3) est >= actual
- If any ticket fails → raise as alert in the daily report
- JIRA instance: `madhuraka-godahewa.atlassian.net`, auth via `kai@nustechnology.com` (config/.jira-config.json)
- Sheet: `1PHW76CuJ7nEJ3bU150iIVFsVXrQOmP5lVKgfI4ESR7I` (Maddy task log)

🔴 **Skipped again 2026-06-24:** the 06:08 run omitted this entirely (no "Maddy JIRA" section in the report) despite "never skip." Ran on recheck when user asked directly "are devs filling Workstream/JIRA correctly" — same class of failure as [[feedback_no_dated_scan_scripts]] (a rule written in memory but not enforced by anything that runs automatically). Treat absence of this section as itself a red flag when reviewing any day's report.

🔴🔴 **Skipped a THIRD time 2026-06-25:** the 06:13 run AND the subsequent full recheck (which re-ran ~10 other pieces: Sheets, Fountain, Matrix, Upwork, Trello, OhCleo, Philip, Aysar) both omitted this section. User had to call it out again: "again, where Maddy monitor !!!" — the recheck session re-verified Sheets/Workstream data for all 6 PHP-team devs but never thought to check whether the Maddy JIRA piece specifically had run, because checking "is data present" was scoped to fixing BLOCKED/expired markers, not to verifying every mandatory sub-piece exists in the final document. **Concrete fix:** before declaring ANY daily-report run (full or recheck) finished, grep the final report file for the literal string "Maddy JIRA" — if absent, run `node scripts/maddy-jira-tasklog-check.js --week <date>` and insert it before finishing, every single time, no exceptions.

**Workstream cross-check added 2026-06-24:** also diff JIRA worklogs (`GET /rest/api/3/issue/{key}/worklog`, filter `started >= weekStart`) against the Workstream "Xtreme Soft Solutions" project (`cmpqc1v7v00ahtk1vs1817xt8`) for the same week. Confirmed: Workstream `employeeName` is always **"LongVV"**, never literally "Kai" — matches `config/.jira-config.json` team mapping (Kai persona → internal_name LongVV). LongVV logs Workstream in a **Wed/Thu batch**, not daily — 0h Mon-Tue is normal, only flag if still empty by Friday. Each week's batch has hit exactly 16h for the last 3 weeks (consistent with the 16h/week part-time target, see [[feedback_longvv_consolidated]]) — the unresolved 16h-vs-40h question in [[feedback_maddy_kai_longvv_identity_and_quality_escalation]] is NOT resolved by this (still ask user before changing the threshold), this just confirms the LongVV=Kai identity mapping itself.

**Also check actual communication, not just hours (added 2026-06-25):** Running the hours-cross-check script alone is NOT a complete Maddy check. User pushed back: "ko đúng, có message gì Slack ko? Jira có update gì ko ? Cần kiểm tra xem communication sao, hứa hẹn gì" (not enough — check Slack messages, JIRA updates, communication, what's been promised). Every Maddy check must ALSO:
1. Search Xtreme Soft Solutions Slack (`search.messages?query=after:{weekStart}`) for the Kai↔Madhuraka DM — read actual message content, not just "Kai active".
2. Pull JIRA comments (`GET /rest/api/3/issue/{key}/comment`) for any ticket touched that week — look for: client-reported issues awaiting a reply, estimates/commitments Kai just gave (e.g. "My estimate is 16 hours") that should be tracked for follow-through, and any client question with no reply 24h+.
3. Report these as a "Maddy — communication & open commitments" subsection: what was asked, what was promised/answered, and whether it's still open. This applies even when the hours-check shows all tickets ✅ — a clean hours check says nothing about whether a client question is sitting unanswered.

🔴🔴🔴🔴 **Root cause found 2026-07-15 — script reads a STALE data source, not "skipped":** `maddy-jira-tasklog-check.js --week 2026-07-14` returned `"No ticket entries in this week"`, and the daily report stated LongVV "hasn't logged any Maddy-specific JIRA entries this week." User rejected this immediately ("totally wrong, check again, Maddy need closely report"). Live check found the script's premise is broken, not just its execution: it extracts ticket keys (`LIFM2-xxx`) from the **Google Sheet** Maddy task log (`SHEET_ID='1PHW76CuJ7nEJ3bU150iIVFsVXrQOmP5lVKgfI4ESR7I'`) — but per [[reference_workstream]], ALL projects (Maddy included) fully migrated to Workstream 2026-07-13, and this Sheet is no longer being filled. Meanwhile live `GET {api_base}/review/week?projectId=cmpqc1v7v00ahtk1vs1817xt8&date=2026-07-14` showed LongVV had logged 4 real task rows referencing 3 tickets that week (LIFM2-450: 4h across Jul13+14, LIFM2-449: 1h, LIFM2-409: 1h, `reviewStatus: NotRequired` on all — Maddy has no configured reviewers, `reviewers: []`, so this project is `need_review=false` per [[feedback_workstream_report_needs_dev_reviewer_hours_and_status]]). **Unresolved as of 2026-07-15: `maddy-jira-tasklog-check.js` itself has NOT been updated** to pull ticket refs from Workstream's `task` field instead of (or in addition to) the abandoned Sheet — it will keep returning false "no entries" until fixed. Next time this runs, do not trust its "no ticket entries" output at face value; cross-check live `/review/week` `task` field text for `[A-Z][A-Z0-9]+-\d+` patterns before concluding no JIRA activity happened.

🔴🔴🔴 **Skipped a 4TH time 2026-07-03:** morning report ran Slack (keyword search only, "Kai progress report... QA feedback done") + JIRA hours table, auto-completed Trello "Maddy" — but never pulled the Kai↔Madhuraka 1:1 DM directly. User called it out again ("Maddy monitor is missing... customer complain"). Deep recheck found a real open item the keyword search missed entirely: client (Madhuraka) sent an urgent hotfix request + "create a hotfix against master" via DM on 06-30 (not JIRA), chased with "Are you there?" on 07-01, and the resulting hotfix PR #513 (LIFM2-409, branch `hotfix/product-custom-payout`) has real bugs flagged by automated code review still unconfirmed-fixed as of 07-03. **Concrete fix, same as the email-scan/sheets-script lesson:** a rule that only lives in memory text and isn't enforced by a checklist gate gets skipped under time pressure, repeatedly. Before marking Trello "Maddy" complete, always fetch full `conversations.history` (not `search.messages` keyword hits) on the Kai↔Madhuraka DM channel for the report window, not just a topical search — this is the specific step that keeps getting dropped.
