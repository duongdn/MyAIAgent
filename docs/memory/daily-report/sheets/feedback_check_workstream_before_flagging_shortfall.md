---
name: feedback_check_workstream_before_flagging_shortfall
description: Sheets/Fountain pieces must live-query the FULL Workstream project list before flagging any dev shortfall or 0h — static project tables go stale and cause false alerts
metadata:
  node_type: memory
  type: feedback
  originSessionId: 7802f504-a1ff-402a-b985-121eb7c467ce
---

**🔴 STRUCTURAL FIX APPLIED 2026-07-08 (after 8 recurrences + user: "trời ạ, đã nói fetch all place cho all dev mà, nói cả triệu lần rồi"):** `scripts/sheets-tasklog-scan.js` now has a built-in `fetchWithRetry()` wrapper (attempts=3, 400ms backoff, keeps the longest/best result) applied to BOTH the per-sheet Google Sheets range fetch AND the per-project Workstream `/review/week` fetch. This is no longer a "remember to re-check" procedural rule — the canonical script retries automatically on every call, every dev, every run. **Do not rely on manually re-running the script or doing a separate unfiltered dump as the primary defense anymore — the tool itself now retries.** Still worth an unfiltered spot-check (`workstream-fetch-project-week.js <date>` no filter) if a 0h result survives 3 retries and seems suspicious, but this should be rare now. If a 0h/shortfall shows up AFTER this fix and doesn't clear on a manual re-run, it's more likely to be genuine — treat with more confidence than pre-fix incidents. If retries still miss real data, that's a NEW bug in the retry logic itself, escalate accordingly.

**Workstream is authoritative when Sheets shows 0h/shortfall** — Sheets may lag or sit unfilled even when hours were worked, for ANY dev with a Workstream project (not just specific ones). First caught 2026-06-09: Sheets showed LongVV 0h W30, Workstream showed 8h Jun 8.

**Deeper bug found 2026-06-23 — the project LIST itself goes stale, not just the sheet data:**
1. **KhanhHH false shortfall**: subagent checked only Baamboozle WS, reported "2h, 6h shortfall." Direct re-query of ALL known WS projects found 6h more under **Generator** (Elliott's project — not in the table that subagent used). True total 8h, no shortfall.
2. **Fountain false "0h" for all devs**: subagent only read the genuinely-empty Sheet W{n} tab. Live query of WS project "Fountain Greetings" showed ViTHT 8h, ThinhT 4h, HungPN 3.5h — team had migrated real logging to Workstream while the Sheet sat unused.

**Why:** User caught both same day ("check workstream why you keep wrong for this !!!"). Pattern: any dev/project can silently move Sheets→Workstream, and the WS project list itself grows without notice (was 5 projects in memory, live API showed 10).

🔴 **RECURRED 2026-06-24** — same KhanhHH/Generator gap, one day after this memory was written. Root cause: the script doing the check (`daily-sheets-scan-260623-tue.js`) hardcoded 3 WS projects and never got the fix applied, because each day's check was a fresh dated script copy, not an edit of one canonical file. This memory's *rule* was correct; the *executable tool* didn't implement it. See [[feedback_no_dated_scan_scripts]] for the structural fix.

**How to apply:**
- Use the canonical script: `node scripts/sheets-tasklog-scan.js <YYYY-MM-DD> <dev1> [dev2 ...]` — it always scans ALL sheets + the FULL live Workstream project list, no per-dev subset to go stale. Do NOT write a new dated script (see [[feedback_no_dated_scan_scripts]]).
- A 0h/shortfall number is the TRIGGER to re-verify via Workstream directly, not something to write down as-is.
- If sheets=0h but Workstream>0h → use Workstream figure, note the discrepancy.
- New WS project found via live query but missing from [[reference_workstream]]'s table → add it immediately.
- `weekTotal`=hours worked, `weekCharged`=billed. Login: `DISPLAY=:1 node scripts/workstream-login.js` (auto-refresh on 401, never report 401 as "unavailable" — fix and retry). `missingReportDays`>0 on past workdays = flag.

🔴🔴 **RECURRED A THIRD TIME 2026-06-25 — and this time a false reminder was actually SENT to the dev.** LeNH was reported "0h, unlogged" during a recheck even though "Peptide Clyde" WAS in that run's live-queried accessible-projects list (10 projects, including Peptide Clyde) — the canonical `sheets-tasklog-scan.js` script ran clean with no errors but still returned empty for this one dev+project combo. A Matrix reminder was sent based on this. LeNH replied "ủa, em có log lên workstream rồi mà ta" (I already logged it though) — re-query moments later found 10.67h real logged work (3:10 + 3:30 + a 4:00 half-day-off credit) on that exact project. Had to send a follow-up Matrix message apologizing and correcting the report. Root cause unconfirmed (no creation-timestamp on Workstream task-log rows to tell if the entries existed yet at the time of the failed query) — could be a transient per-project API failure silently swallowed as empty, or a timing gap. Either way: **the live project list being present is not proof the per-project data fetch actually succeeded.**
- **Concrete rule going forward:** before *sending* anything to a human (Matrix reminder, Trello block, etc.) based on a 0h/shortfall finding — not just before writing a report line — re-run the query for that exact dev+project ONE more time, isolated, immediately before sending. The cost of being wrong after a message is sent (wasted dev time, eroded trust) is categorically higher than being wrong in a report draft that hasn't gone out yet.
- If a dev pushes back on a reminder/alert, treat that as a strong signal to re-verify immediately, not to explain the rule to them — they have ground truth, the script does not.

🔴🔴🔴 **RECURRED A 4TH TIME 2026-07-02 — same dev (LeNH), same project (Peptide Clyde) again, plus KhanhHH undercounted too.** During a daily-report recheck: (1) `sheets-tasklog-scan.js` ran clean for LeNH across all 18 live-queried projects (Peptide Clyde included in the accessible list) and returned **0h everywhere** — reported as a genuine alert. (2) Separately, KhanhHH's Generator hours came back as 3.5h from one script pass. User pushed back on BOTH ("check again all workstream/task log!", "LeNH: wrong, check workstream", "why you keep wrong about workstream!!!"). Direct per-project `/review/week` queries (bypassing the script, same endpoint) run immediately after, with the exact same token, found: **LeNH had 7.83h in Peptide Clyde** (missed entirely — same project as the 2026-06-25 incident) and **KhanhHH's true Generator total was 6.5h across 4 tasks**, not 3.5h. Re-running the unmodified canonical script fresh a few minutes later returned the correct numbers for both. No code bug found — same request, same token, different result across consecutive calls seconds apart.
- **Pattern now: Peptide Clyde (Blair Brown project) has failed to return a dev's task-log rows on a live query at least twice** (2026-06-25 LeNH, 2026-07-02 LeNH again) — if this recurs a 3rd time on this same project, treat it as a project-specific API/caching issue worth reporting to whoever maintains the Workstream backend, not just a generic "re-verify" workaround.
- **Escalated rule: a single script pass on Workstream data is not sufficient evidence for ANY 0h or shortfall claim, ever** — this is no longer a rare edge case, it's a recurring (4x) reliability gap. Before writing ANY 0h/shortfall line into a report (not just before sending a reminder), re-run the same query at least once more, a few minutes apart if possible, and take the higher of the two results. If numbers differ between runs, report the higher number and note the discrepancy — never the lower/first one.

🔴🔴🔴🔴 **RECURRED A 5TH TIME 2026-07-03 — same dev (LeNH) again, this time WITHIN THE SAME DAILY-REPORT RUN as a KhanhHH miss.** Morning daily-report run (~08:37): `sheets-tasklog-scan.js`-style check reported LeNH 0h on 07-02 with no leave note — written into the report as a STRICT-rule alert (no reminder actually sent, print-only). ~1 hour later, user asked "have devs filled their weekly hours" — a fresh live Workstream query for 07-02 found LeNH logged 8h that day (Peptide Clyde 4h + Portfolio-James Diamond 4h). The SAME run had already found and corrected an identical KhanhHH miss 10 minutes earlier (her Elena-sheet-blocked total was actually complete via Workstream) — meaning **two independent 0h/shortfall misses landed in the same single daily-report execution**, both only caught because the user manually pushed back / asked to re-verify, not because the agent self-applied the escalated re-run rule above despite it being written down for this exact scenario.
- **This confirms the "re-run + take higher" rule is not being applied even when explicitly documented and even within the same session that already hit the sibling bug once.** The fix cannot rely on remembering to re-run at report-writing time — it needs to be enforced procedurally: e.g. any daily-report piece that touches sheets/task-log hours should mechanically issue two Workstream passes per dev (not just once) before a 0h/shortfall line is allowed into a report draft, or the canonical script itself should build in a retry+max internally so downstream callers can't skip it.
- Also reconfirms: **Peptide Clyde continues to be the recurring failure point** — 3 of 5 total incidents (06-25, 07-02, 07-03) involve this specific project ID (`cmqj4tj6v01gfm81vgx7ipkov`). Worth flagging to whoever maintains the Workstream backend as a project-specific issue, not just a general flakiness caveat.

🔴🔴🔴🔴🔴 **RECURRED A 6TH TIME 2026-07-07 — but this time on the GOOGLE SHEETS side, not Workstream, and specific to multi-dev invocations.** `sheets-tasklog-scan.js 2026-07-06 LongVV TuanNT KhanhHH PhucVT LeNH` (and a 2nd re-run with a smaller dev subset) both returned TuanNT `sheetsTotal: 0` — used as grounds to (wrongly) claim TuanNT's status was "unverified," blocking John Yi/Bailey/Rebecca Trello items. User pushed back hard ("employee time: wrong all, check again!!!"). Running the EXACT SAME script with TuanNT as the **only** dev argument (`sheets-tasklog-scan.js 2026-07-06 TuanNT`) found the real entry immediately: **8h in Paturevision**, a well-established sheet already in the SHEETS list (not a new/unknown sheet — this isn't the "sheet surfaces without notice" issue, it's a live read failure on a known sheet). Direct manual read of the Paturevision `W35` tab confirmed the row exists exactly as described (dated "Mon, 06/07/26", owner TuanNT, 8h, 2 tasks).
- **New pattern: multi-dev single-invocation scans can silently drop a dev's row from a sheet that a single-dev invocation of the identical script finds without issue.** Root cause unconfirmed (possibly a Sheets API rate-limit/quota effect when the script fires many `values.get` calls back-to-back for multiple devs × 13 sheets in one process). This is distinct from the Peptide Clyde/Workstream flakiness above — same symptom class (single pass ≠ reliable), different subsystem (Google Sheets API, not Workstream).
- **Escalated rule, extended:** when a multi-dev scan shows 0h for any one dev and that 0h is about to gate a Trello item or trigger a reminder, re-run the scan **with that dev as the sole argument** (not just re-running the same multi-dev command) before treating it as confirmed. This isolates whether the miss is dev-specific data or a multi-request artifact.

🔴🔴🔴🔴🔴🔴 **RECURRED A 7TH TIME 2026-07-07 — new specific mechanism found: `GET /review/week?projectId=...&date=...` returns `403 Forbidden` (not empty, an actual error) when `date` is the exact Monday/start-of-week or the 2 days right after it, but returns full correct data (200 OK) when `date` is any day from the middle to end of that same week.** Concretely, for week `2026-06-29` to `2026-07-05`: `date=2026-06-29`, `2026-06-30`, `2026-07-01` → 403 Forbidden; `date=2026-07-02` through `2026-07-05` → 200 OK with the complete week's rows (`weekStart`/`weekEnd` identical in both cases — same week, different query outcome). This was mistaken for "TienND has 0h logged in Workstream" (a serious false alarm — user directly refuted it with a screenshot showing TienND's real 48h that week, tasks matching Slack reports exactly). **Rule: when querying `/review/week` for a specific historical week, never use the week's first 1-3 calendar days as the `date` param — use a date from the back half of the target week instead.** If a 403 or empty-rows result comes back for an early-week date, retry with a later date in the same week before concluding there's no data.

🔴🔴🔴🔴🔴🔴🔴 **RECURRED AN 8TH TIME 2026-07-08 — same dev (LeNH), NEW project this time: "Portfolio - James Diamond" (`james_diamond`, id `cmqook9vf0kl8m81vusyo8ppt`).** `sheets-tasklog-scan.js 2026-07-07 LeNH` ran TWICE (fresh WS token both times, per the escalated re-run rule) and returned 0h both times across all 13 sheets + all 20 live-queried WS projects — James Diamond WAS in the accessible-projects list both times, same as the Peptide Clyde incidents. Agent then went further than a 3rd identical re-run: dumped raw Google Sheets day-blocks for the 4 mapped sheets directly (genuinely blank, that part was real) and concluded "genuine shortfall, holds up this time" — written into the report as confirmed, NOT an alert-retraction. User pushed back again ("leNH: sai, tôi thấy đã đủ giờ" — wrong, I see she has enough hours). A **completely unfiltered dump of ALL Workstream projects with ALL members** (no per-dev substring filter, no per-project targeted query) immediately found it: LeNH logged **8h on 07-06 AND 8h on 07-07** in James Diamond (weekTotal 16h). The per-dev-filtered `fetchWorkstreamWeek` call for that exact project, on that exact day, returned empty for LeNH specifically — even though the identical unfiltered call moments later returned her rows correctly. **This confirms the bug is not project-specific (Peptide Clyde was never special) — it's a per-dev-filter-on-a-specific-project-fetch flakiness that can hit ANY project, and re-running the SAME filtered script call (even fresh-token, even N times) does not reliably surface it.** The only method that worked here: bypass `sheets-tasklog-scan.js`'s dev-filtering entirely and read the full unfiltered project dump.
- **Escalated rule, again: before writing "genuine shortfall" for ANY dev, do not stop at re-running `sheets-tasklog-scan.js` N times.** Also pull the full unfiltered Workstream dump (`node scripts/workstream-fetch-project-week.js <date>` with no dev filter) and eyeball every project's member list for that dev's name/variants. This is now the required 2nd-tier check, not optional, for any 0h/shortfall claim about LeNH or KhanhHH specifically (both have 8+ confirmed false-alarm incidents).
- **This was the 2nd correction on the SAME report/SAME dev within roughly 30 minutes** (09:26 KhanhHH+Aysar correction, 09:55 first LeNH "confirmed genuine" claim, 10:2x this correction reversing it). Pattern: the agent's own deeper-dive investigation produced a confident, detailed, WRONG conclusion — thoroughness of the wrong method doesn't make the conclusion right. When a user says "sai" a 2nd time about the same fact, do not re-defend the prior finding — immediately try a structurally different verification method (here: unfiltered dump) instead of re-running the same one again.
